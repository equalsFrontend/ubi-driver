/*jshint -W099*/

/**
 * iMetric
 * @module presentation/calendar.services
 * @author Alex Boisselle
 */

(function(){

    "use strict";

    var bingServices = angular.module('UBIDriver.components.calendar.services', [])

    .factory('CalendarService', ['$q', '$rootScope', function($q, $rootScope){
        return {
            init: function(){

            }
        };
    }]);
})();